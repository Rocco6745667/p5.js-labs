//Rocco Ali 218008847

//● setup() is called once when the program starts
function setup() {
    // this creates the canvus 
    createCanvas(400, 400); //width and length
      // Turn off the draw loop.
    noLoop();
  
    describe('A white half-circle on the left edge of a gray square.');
  }
  
  // called directly after setup() 
  function draw() {
    // creates the background
    background(120);
    
    //prints Rocco Ali in the console
    print("Rocco Ali")
  }